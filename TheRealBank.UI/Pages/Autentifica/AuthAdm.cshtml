@page
@model TheRealBank.UI.Pages.Autentifica.AuthAdmModel
@{
    ViewData["Title"] = "Acesso Administrativo";
}

<style>
    .auth-container {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .login-card {
        background-color: var(--bank-dark);
        border: 1px solid rgba(255, 0, 0, 0.15); 
        border-radius: 15px;
        padding: 40px;
        width: 100%;
        max-width: 450px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }

    .login-header {
        text-align: center;
        margin-bottom: 30px;
    }

        .login-header h2 {
            color: white;
            font-weight: 700;
            letter-spacing: -1px;
        }

        .login-header p {
            color: var(--text-light-on-dark);
        }

    .login-icon {
        font-size: 50px;
        color: #dc3545; 
        margin-bottom: 15px;
    }

    .form-label-custom {
        color: var(--text-light-on-dark);
        font-weight: 500;
        margin-bottom: 8px;
        display: block;
    }

    .form-control-custom {
        background-color: rgba(255, 255, 255, 0.05) !important;
        border: 1px solid rgba(255, 255, 255, 0.15) !important;
        color: white !important;
        padding: 12px 15px;
        border-radius: 8px;
        font-size: 1rem;
        transition: all 0.3s ease;
    }

        .form-control-custom:focus {
            background-color: rgba(255, 255, 255, 0.1) !important;
            border-color: #dc3545 !important; 
            box-shadow: 0 0 0 3px rgba(220, 53, 69, 0.25) !important;
        }

    .btn-login-adm {
        background-color: #dc3545; 
        color: white;
        font-weight: 600;
        padding: 12px;
        font-size: 1.1rem;
        border-radius: 10px;
        width: 100%;
        transition: all 0.3s ease;
        margin-top: 20px;
        border: none;
    }

        .btn-login-adm:hover {
            background-color: #c82333;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(220, 53, 69, 0.3);
        }

    .auth-links {
        text-align: center;
        margin-top: 25px;
    }

        .auth-links a {
            color: var(--text-light-on-dark);
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.2s;
        }

            .auth-links a:hover {
                color: white;
            }
</style>

<div class="container auth-container">
    <div class="login-card">
        <div class="login-header">
            <i class="fa-solid fa-user-shield login-icon"></i>
            <h2>Área Restrita</h2>
            <p>Acesso exclusivo para administradores.</p>
        </div>

        @if (!ModelState.IsValid)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fa-solid fa-triangle-exclamation me-2"></i>
                <strong>Acesso Negado:</strong> Credenciais inválidas.
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <form method="post">
            <div class="mb-3">
                <label asp-for="Input.Email" class="form-label-custom">
                    <i class="fa-solid fa-id-badge me-1"></i> Credencial Administrativa
                </label>
                <input asp-for="Input.Email" type="email" class="form-control form-control-custom" placeholder="admin@therealbank.com" required>
                <span asp-validation-for="Input.Email" class="text-danger small mt-1 d-block"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Input.Password" class="form-label-custom">
                    <i class="fa-solid fa-key me-1"></i> Chave de Acesso
                </label>
                <input asp-for="Input.Password" type="password" class="form-control form-control-custom" placeholder="••••••••" required>
                <span asp-validation-for="Input.Password" class="text-danger small mt-1 d-block"></span>
            </div>

            <button type="submit" class="btn btn-login-adm">
                <i class="fa-solid fa-unlock me-2"></i> Acessar Painel
            </button>
        </form>

        <div class="auth-links">
            <a asp-page="/Autentifica/Auth">
                <i class="fa-solid fa-arrow-left me-1"></i> Voltar para Login de Usuário
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}