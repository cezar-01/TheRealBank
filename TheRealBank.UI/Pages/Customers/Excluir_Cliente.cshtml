@page "{cpf}"
@model TheRealBank.UI.Pages.Customers.Excluir_ClienteModel
@{
    ViewData["Title"] = "Excluir Cliente";
    Layout = "/Pages/Shared/_Layout.cshtml"; 
}

<style>
    .hero-section {
        background: linear-gradient(135deg, #f2f7fa 0%, #ffffff 100%);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: calc(100vh - 120px);
        padding: 40px 0;
    }

    .confirm-container {
        text-align: center;
        background-color: white;
        padding: 40px 50px;
        border-radius: 16px;
        box-shadow: 0 15px 40px rgba(0,0,0,0.08);
        border: 1px solid #e0e8f0;
        max-width: 600px;
        width: 90%;
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInSlideUp 0.8s ease-out forwards;
    }

    @@keyframes fadeInSlideUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .confirm-header {
        text-align: center;
        margin-bottom: 25px;
    }

    .confirm-icon {
        font-size: 3rem;
        color: #dc3545; 
        margin-bottom: 15px;
    }

    .confirm-header h1 {
        color: var(--bank-dark); 
        font-weight: 700;
        font-size: 2.2em;
        margin: 0;
    }

    .confirm-container p {
        color: #556278;
        font-size: 1.15em;
        line-height: 1.7;
        margin-bottom: 25px;
    }

    .client-info {
        background-color: #f8f9fa; 
        border: 1px solid #e9ecef;
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 30px;
        text-align: left;
    }

        .client-info strong {
            color: var(--bank-dark);
            display: block;
            font-size: 1.1em;
        }

        .client-info span {
            color: #556278;
            font-size: 1.05em;
        }

   
    .action-buttons {
        display: flex;
        justify-content: center;
        gap: 20px; 
    }

        .action-buttons .btn {
            font-weight: 600;
            font-size: 1.1em;
            padding: 12px 30px;
            border-radius: 8px;
            min-width: 150px; 
        }

            .action-buttons .btn i {
                margin-right: 8px;
            }

    .btn-danger {
        transition: all 0.3s ease;
    }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(220, 53, 69, 0.2);
        }

    .btn-outline-secondary {
        border-width: 2px;
        transition: all 0.3s ease;
    }

        .btn-outline-secondary:hover {
            transform: translateY(-2px);
        }

</style>

<div class="hero-section">
    <div class="confirm-container">

        <div class="confirm-header">
            <i class="fa-solid fa-triangle-exclamation confirm-icon"></i>
            <h1>Confirmar Exclusão</h1>
        </div>

        <p>Tem certeza que deseja excluir permanentemente o cliente abaixo? Esta ação não pode ser desfeita.</p>

        <div class="client-info">
            <strong>@Model.Cliente.Nome</strong>
            <span>CPF: @Model.Cliente.CPF</span>
        </div>

        <div class="action-buttons">
            <a class="btn btn-outline-secondary btn-lg" asp-page="/Customers/ExibirClientes">
                <i class="fa-solid fa-xmark"></i> Não
            </a>

            <form method="post" class="m-0">
                <input type="hidden" asp-for="Cliente.CPF" />
                <button type="submit" class="btn btn-danger btn-lg">
                    <i class="fa-solid fa-trash-can"></i> Sim, Excluir
                </button>
            </form>
        </div>
    </div>
</div>
